node_modules

# Environment variables
.env.local
.env*.local

# macOS
.DS_Store
**/.DS_Store

# Build outputs
dist/
build/
*.log

# IDE
.vscode/
.idea/

# Temporary files
*.tmp
*.temp
prompts_used_to_create_frontent.txt

# Terraform
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
# Covers timestamped backups (terraform.tfstate.<epoch>.backup) too, which the
# two narrower patterns previously here did not match.
infrastructure/terraform/*.tfstate*
infrastructure/terraform/*.tfvars
infrastructure/terraform/.state-snapshots/
infrastructure/terraform/lambda/*/node_modules/
infrastructure/terraform/lambda/*/*.zip
